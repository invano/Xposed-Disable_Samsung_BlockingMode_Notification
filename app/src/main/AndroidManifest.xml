<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.invano.disableblockingmodenotification">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="com.invano.disableblockingmodenotification.Settings"
            android:label="@string/app_name"
            android:theme="@style/Theme.UserDialog" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data android:value="true" android:name="xposedmodule"/>
        <meta-data android:value="2.0*" android:name="xposedminversion"/>
        <meta-data android:value="Disable the Samsung BlockingMode notification" android:name="xposeddescription"/>
    </application>

</manifest>
